language: cpp
compiler: clang

matrix:
    include:
    - os: linux
      dist: trusty
      sudo: false
    - os: osx
      osx_image: xcode9.4

addons:
    apt:
        sources:
        - llvm-toolchain-trusty-6.0
        - ubuntu-toolchain-r-test

        packages:
        - g++-8
        - llvm-6.0
        - llvm-6.0-dev
        - libllvm6.0
        - libmpfr-dev
        - libmpfr4
    homebrew:
        packages:
        - llvm@6
        update: true

script:
    - if [ "$TRAVIS_OS_NAME" == "osx" ];
        then PATH="/usr/local/opt/llvm@6/bin:$PATH" LLVM_CONFIG=llvm-config make -j tester;
      else CXX=g++-$GCC_VERSION CC=gcc-$GCC_VERSION make -j ci;
      fi

notifications:
    email: false

after_success:
    - zip -r $TRAVIS_OS_NAME-x64 build/sysroot

deploy:
    provider: releases
    api-key: ${GITHUB_OAUTH_TOKEN}
    file: $TRAVIS_OS_NAME-x64.zip
    skip-cleanup: true
    on:
        all_branches: true
        tags: true
