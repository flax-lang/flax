language: cpp
compiler: clang
before_install: sudo apt-get update

install:
    - build/get-dependencies.sh
    - echo "LLVM $LLVM_VERSION"
    - sudo apt-get update
    - sudo apt-get install libllvm3.6 libllvm3.6-dbg llvm-3.6 llvm-3.6-dev llvm-3.6-runtime

script: ./capri

