// test.flx
// Copyright (c) 2014 - The Foreseeable Future, zhiayang@gmail.com
// Licensed under the Apache License Version 2.0.

import Foundation
import Utilities

func main(argc: Int32, argv: Int8**) -> Int64
{
	var pointer: Int64* = malloc(8 * 4) as Int64*
	memset(pointer as Int8*, 0, 8 * 4)

	pointer[0] = 40012
	pointer = pointer + 1
	pointer[0] = 69313

	while true
	{
		printf("haha")
	}


	var y: String = "Hello, World!"
	printf("%s, %d\n", y.data, pointer[-1])

	var d: Int32 = 40
	d += 5913
	free((pointer - 1) as Int8*)
	return 0
}
